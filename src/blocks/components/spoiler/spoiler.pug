-
    const defaultSpoilerProps = {
        classes: '',
        title: '',
        text: '',
    }

mixin spoiler(props = defaultSpoilerProps)
    -
        const checkProps = {
            classes: props.classes ? props.classes : defaultSpoilerProps.classes,
            title: props.title ? props.title : defaultSpoilerProps.title,
            text: props.text ? props.text : defaultSpoilerProps.text,
        }

    .spoiler(class= checkProps.classes)&attributes(attributes)
        .spoiler__intro
            .spoiler__title!= checkProps.title
            .spoiler__icon
                svg
                    use(xlink:href=`img/sprites/sprite.svg#plus`)
        .spoiler__content
            .text-edilor!= checkProps.text